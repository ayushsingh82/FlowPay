{
  "name": "@flowpay/sdk",
  "version": "0.1.0",
  "description": "FlowPay TypeScript SDK — three calls (deposit, pay, watchVault) that build real Sui transactions against the FlowPay Move package.",
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run examples/spend.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["sui", "flowpay", "payments", "wallet", "defi"],
  "license": "MIT",
  "dependencies": {
    "@mysten/sui": "^2.19.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3"
  }
}

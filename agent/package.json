{
  "name": "flowpay-agent",
  "version": "0.1.0",
  "description": "FlowPay off-chain agent service — risk-monitoring + rate-routing for a multi-asset Sui payment wallet.",
  "type": "module",
  "module": "src/server.ts",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "bun-types": "^1.1.0",
    "typescript": "^5.5.0"
  }
}
